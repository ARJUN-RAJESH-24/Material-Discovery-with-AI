# Model Weights

This folder stores trained models for the Material Discovery with AI project.

- **cgcnn_model.pt** → CGCNN trained weights  
- **baseline_model.pkl** → Baseline ML model  
- **versioning**: Include date or git commit when saving new models.
